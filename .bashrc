# These are supposedly to define colors in xterm
# I think the only necessary change is the alias, the others, I don't think they
# work.
alias ls="ls --color"

# Running node binaries without a global install
alias npm-exec='PATH=$(npm bin):$PATH'

export CLICOLOR=TRUE
export LSCOLORS=Cxfxbxdxcxegedabagacad

